function coworking(){

    var exibir = window.document.getElementById("result")
    exibir.innerText = `lista de coworking`
}

function empresarial(){

    var exibir = window.document.getElementById("result")
    exibir.innerText = `lista das empresas`
}

function parceiros(){

    var exibir = window.document.getElementById("result")
    exibir.innerText = `lista dos parcerios`
}